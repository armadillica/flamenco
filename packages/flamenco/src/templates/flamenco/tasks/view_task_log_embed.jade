| {% if page_idx != 1 %}
a(href="{{ url_for(request.endpoint, project_url=project.url, task_id=task_id, page=1) }}")
    | ... first logs ...
| {% endif %}
| {% if has_prev_page %}
a(href="{{ url_for(request.endpoint, project_url=project.url, task_id=task_id, page=page_idx-1) }}")
    | ... earlier logs ...
| {% endif %}

| {% for task_log in logs['_items'] %}
|     {% for line in task_log.log.split('\n') %}
p.task-log-line {{ line }}
|     {% endfor %}
| {% endfor %}

| {% if has_next_page %}
a(href="{{ url_for(request.endpoint, project_url=project.url, task_id=task_id, page=page_idx+1) }}")
    | ... later logs ...
| {% endif %}
| {% if page_idx != last_page_idx and last_page_idx > 1 %}
a(href="{{ url_for(request.endpoint, project_url=project.url, task_id=task_id, page=last_page_idx) }}")
    | ... last logs ...
| {% endif %}
